# Claude Code 指示書 - 記事チェッカープロジェクト

このファイルはClaude Codeがプロジェクトのコンテキストを把握するための指示書です。

## プロジェクト概要

WordPressで作成された記事を、ニュースメディア(JJ / VERY / CLASSY. / STORY / bis / Mart / 美ST)に配信する前に、外注メンバーが自分でセルフチェックするためのツールを開発します。

詳細な背景・要件は `README.md` を参照してください。**実装に着手する前に必ずREADME.mdを読んでください。**

> **重要 — 変更ログの運用:** 機能追加・仕様変更・バグ修正を行ったら、**必ず `CHANGELOG.md` の先頭（累積まとめの上）に日付降順で追記する**こと。コミット前に忘れず更新する。

---

## 技術スタック

| 項目 | 採用するもの | 理由 |
|---|---|---|
| 言語 | プレーンJavaScript(ES2017相当) | 古いChromeでも動作させるため。トランスパイル不要 |
| フレームワーク | なし | 軽量・依存ゼロを優先 |
| 配布形態 | ブックマークレット + 外部JS | インストール不要、ルール更新を管理側で完結させるため |
| ホスティング | Cloudflare Pages または GitHub Pages | 無料運用が前提 |
| ビルドツール | 使わない(または最小限) | 外注のPC環境に依存しないため |

**禁止事項:**

- npm依存パッケージの追加(node_modulesを膨らませない)
- ES2020以降の構文(optional chaining `?.` / nullish coalescing `??` も避ける)
- TypeScript(プレーンJSのみ)
- jQueryなどの古いライブラリ(不要)

---

## ディレクトリ構成(想定)

```
記事チェッカー/
├── README.md          このプロジェクトの全体像
├── CLAUDE.md          この指示書
├── docs/              詳細仕様・調査メモ
├── src/
│   ├── bookmarklet.js ブックマーク登録用の短いローダー
│   ├── checker.js     チェックロジック本体
│   └── config.json    NGワード・媒体マッピング・関連リンク規定数等
├── dist/              ビルド成果物(あれば)
└── test/              テスト用のサンプルHTML
```

実装はsrc配下に集中させてください。

---

## 実装方針

### bookmarklet.js (ローダー)

- ブックマークに登録する短いJSスニペット
- 外部JS(checker.js)を動的にロードする
- キャッシュバスター(`?v=Date.now()`)を必ず付ける(ルール更新が即反映されるように)

### checker.js (本体)

- WordPressプレビュー画面のDOMを読み取って各種チェックを実行
- 結果をオーバーレイパネルで画面右側に表示
- 既存パネルがあれば削除してから再描画(2回目以降のクリックで重複しないように)

#### チェック項目(README.md セクション4参照)

優先実装順:

1. 媒体とドメインの整合性(対応表は `config.json` に持つ)
2. 関連リンク数・タイトル先頭の▶記号
3. 残置文言検出(NGワードリストとの照合)
4. 画像設定漏れ(段落単位)
5. 時勢ワード検出(タイトル・本文)
6. HTML異常検出

### config.json

ルール類は全てここに集約。コードを触らずにルール更新できるようにする。

```json
{
  "media_mapping": {
    "JJ": "jj-jj.net",
    "VERY": "veryweb.jp",
    "CLASSY.": "classy-online.jp",
    "STORY": "storyweb.jp",
    "bis": "bisweb.jp",
    "Mart": "mart-magazine.com",
    "美ST": "be-story.jp"
  },
  "ng_phrases": [],
  "time_words": [],
  "related_links_count": 5,
  "related_link_prefix": "▶"
}
```

NGワード等は実データを見ながら埋めていく。

---

## 進め方(Phase別)

### Phase 2: DOM構造の確認(最優先・未完了)

実装前にWordPressプレビュー画面のHTML構造を確認する必要があります。Akaneさんに以下を依頼してください:

- WordPressのプレビュー画面で開発者ツールを開き、以下の要素のDOM構造を確認:
  - タイトル行
  - カテゴリ行
  - 段落見出し・本文・画像のtd
  - 関連リンクのタイトルとURL
- ページソース(HTML全体)をtest配下にサンプルとして保存

この情報がないとセレクタが書けないので、推測実装は避けてください。

### Phase 3: MVP実装

DOM構造が確定したら、以下の順で実装:

1. `src/bookmarklet.js` のローダー作成
2. `src/checker.js` のスケルトン(DOM抽出→結果表示パネル)
3. `src/config.json` の媒体マッピングだけ先に投入
4. 媒体とドメインの整合性チェックを最初に実装(実装が単純で効果が大きい)
5. 順次他のチェック項目を追加

### Phase 4以降

実機で運用テスト → フィードバック反映 → 全展開

---

## コーディング規約

- **命名:** snake_case ではなく camelCase(JS慣習に合わせる)
- **コメント:** 日本語OK。なぜそうするか(why)を中心に書く
- **エラーハンドリング:** プレビュー画面以外で実行された場合は静かに何もしない(エラーで止めない)
- **CSP対策:** インラインstyle/scriptは避けたいが、ブックマークレットの性質上、最小限は許容
- **i18n:** UIは日本語固定でOK

---

## テスト方針

- `test/` 配下にプレビュー画面のサンプルHTMLを置く
- ローカルでHTMLを開いてchecker.jsを手動実行できるテストページを用意する
- 自動テストは現時点では不要(コードが小さいため)

---

## 残課題リスト(README.md セクション8より)

実装中に判明したものはここに追記してください。

- [ ] プレビュー画面のHTML構造の確認
- [ ] 関連リンクの規定数(暫定: 5)
- [ ] 削除対象文言の具体例リスト
- [ ] 媒体名表記ゆれの有無(「｜STORY」「| STORY」「|STORY」)
- [ ] 関連リンクが他媒体を含むケースの有無
- [ ] ホスティング先の決定(Cloudflare Pages / GitHub Pages)

---

## 困ったらAkaneさんに確認すること

- DOM構造が不明な要素について推測で実装しない
- 新しいチェックルールを勝手に追加しない(必ず仕様確認)
- ホスティング先を勝手に決めない
- 媒体マッピングの追加・変更は必ず確認

---

## このプロジェクトのゴール

外注メンバーが「プレビュー開く → ブックマーククリック」の2ステップでセルフチェックを完了でき、これまで発生していたミスを納品前に潰せるようになること。
